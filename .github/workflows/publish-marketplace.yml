name: Publish tagged release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  publish:
    uses: ./.github/workflows/publish-release.yml
    with:
      ref: ${{ github.ref }}
    secrets: inherit
